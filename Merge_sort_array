class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:

        idx = m

        for nums in nums2:
            nums1[idx] = nums
            idx += 1
        
        nums1.sort()
